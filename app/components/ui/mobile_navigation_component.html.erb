<div class="md:hidden" data-controller="toggle">
  <div class="p-4 text-2xl relative z-20" data-toggle-target="toggleable" data-action="click->toggle#toggle">
    <i class="fas fa-bars"></i>
  </div>
  <div class="p-4 text-2xl relative z-20 text-gray-200 hidden" data-toggle-target="toggleable" data-action="click->toggle#toggle">
    <i class="fas fa-times"></i>
  </div>
  <div
    class="h-screen w-screen absolute right-0 top-0 z-10 items-center bg-black/80 p-4 hidden"
    data-toggle-target="toggleable"
  >
    <ul class="flex flex-col gap-6 text-right mt-12 text-2xl text-gray-200">
      <li class="font-semibold"><%= link_to t(".properties"), buyer_app_offers_path %></li>
      <li class="font-semibold"><%= link_to t(".portfolios"), buyer_app_portfolios_path %></li>
      <li class="font-semibold"><%= link_to t(".profile"), buyer_app_profiles_path %></li>
      <li class="font-semibold"><%= link_to t(".logout"), destroy_buyer_session_path, method: :delete %></li>
    </ul>
  </div>
</div>
