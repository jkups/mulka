<header class="border-b border-gray-300 sticky top-0 bg-white">
  <div class="max-w-screen-xl mx-auto">
    <div class="flex justify-between">
      <div class="p-4 text-xl font-bold text-green-500 lg:px-0">
        <%= link_to "Mulka", buyer_app_offers_path %>
      </div>
      <%= render Ui::DesktopNavigationComponent.new(user: current_buyer) %>
      <%= render Ui::MobileNavigationComponent.new(user: current_buyer) %>
    </div>
  </div>
</header>
